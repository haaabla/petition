<p>Signees from Berlin:</p>

<ul class="signee-list">
    {{#each signees}}
        <li>
            {{#if url}}
                <a href="http://{{url}}" target="_blank">{{first_name}} {{last_name}}</a>
            {{else}}
                {{first_name}} {{last_name}}
            {{/if}}
            {{#if age}}
                    ( {{age}}
            {{else}}
                {{#if city}}
                    (<a href="/signees/{{city}}">{{city}}</a>)
                {{/if}}
            {{/if}}
            {{#if city}},
                <a href="/signees/{{city}}">{{city}}</a>)
            {{else}}
                {{#if age}}){{/if}}
            {{/if}}
        </li>
    {{/each}}
</ul>
